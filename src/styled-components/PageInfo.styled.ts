import styled from "styled-components";

export const PageInfoStyled = styled.div`
  margin-left: 28px;
  transition: var(--transition-def);

  .page-name {
    font-size: 24px;
    font-weight: var(--fw-bold);
  }
  .page-info {
    color: var(--text-info-color);
    font-size: 16px;
    font-style: italic;
  }
`;
